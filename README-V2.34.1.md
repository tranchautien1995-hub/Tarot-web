# Tarot Practice V2.34.1

UI-only patch based directly on V2.34.

- Fanned deck uses a dense 52-card visual subset; each visible card matches the fixed 4.5 × 7 cm spread-slot size.
- Fixes hover/click flicker at overlapping fan-card seams by keeping hit-box geometry stationary on hover.
- Separates shuffle packet, progress label and shuffle button vertically so text/background no longer overlaps the shuffle animation.
- Reader prompts, API routes and model integration are unchanged from V2.34.
